---
import { serializeJsonLd } from '../lib/json-ld.js';
interface Props { schema?: any; }
const { schema } = Astro.props;
---
{schema && <script type="application/ld+json" set:html={serializeJsonLd(schema)} />}
